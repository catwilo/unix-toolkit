# zsh-setup context

## state
- lib: core, detect, links, pkg, plugins — complete
- optional/mpd: complete
- optional/repair-nix: empty
- configs/debian|mac|shared|termux: empty (dotfiles live in custom_termux)
- packages/debian.env + termux.env: complete
- setup.sh: MISSING — next to build

## pending
1. setup.sh — main entry point (sources libs, calls init_platform, pkg_install_file, link_dotfiles, verify_plugins, optional mpd)
2. repair-nix — define scope or remove
3. configs/ — clarify purpose vs custom_termux

## last-known-good
- all lib/*.sh syntactically clean
